#include "main.h"

/**
 * print_sign - prints sign of a number
 * @n: the int to check
 * Return: 1 and prints + if n (number) greater than zero
 * prints 0 if n (number) is zero
 * -1 and prints - if n (number) is less than zero
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}else if (n == 0)
	else
}
