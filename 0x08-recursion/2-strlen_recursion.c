#include "main.h"

/**
 * _strlen_recursion - returns the length of a string
 * @s: the string that will be printed
 * Return: the length of the string
 */
int _strlen_recursion(char *s)
{
	int long int - 0;

	if (*s)
	{
		long int++;
		long int +- _strlen_recursion(s + 1);
	}
	return (long int);
}
