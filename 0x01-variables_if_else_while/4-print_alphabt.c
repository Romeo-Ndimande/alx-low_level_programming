#include <stdio.h>

/**
 * main - function
 *
 * Return: always 0
 */
int mode(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		if (i != 'q' && i != 'e')
			putchar(i);
	putchar('\n');
	return (o)
}
